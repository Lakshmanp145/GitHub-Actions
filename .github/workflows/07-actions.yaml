name: Checkout #workflow
on: [push] #events like push, crete PR, create an issue
jobs: # a workflow can have multiple pipelines
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout the code 
        uses: actions/checkout@v5
      - name: step2
        run: ls -la
